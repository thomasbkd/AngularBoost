import { TranslateOnSelectDirective } from './translate-on-select.directive';

describe('TranslateOnSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslateOnSelectDirective();
    expect(directive).toBeTruthy();
  });
});
